import React from 'react';
import { View } from 'react-native';

export const Line = () => (
  <View
    style={{
      paddingTop: 40,
      width: '100%',
      borderBottomColor: 'rgba(0,0,0,0.2)',
      borderBottomWidth: 5,
      borderStyle: 'solid',
    }}
  />
);
